<div class="bg-white shadow-md rounded-lg overflow-hidden">
  <img class="w-full h-48 object-cover" src="{{cover_image_url}}" alt="{{title}}">
  <div class="p-4">
    <h2 class="text-xl font-semibold mb-2">{{title}}</h2>
    <p class="text-gray-500 mb-4">Director: {{director}}</p>
    <a href="/film-detail-page/{{id}}" class="bg-blue-500 text-white py-2 px-4 rounded">
        Details
    </a>
  </div>
</div>
